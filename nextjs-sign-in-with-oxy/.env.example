# Oxy API base URL. Use the production endpoint or your own self-hosted Oxy API.
NEXT_PUBLIC_OXY_API_URL=https://api.oxy.so

# (Optional) Override the Oxy auth web app URL. Defaults to https://auth.oxy.so.
# NEXT_PUBLIC_OXY_AUTH_WEB_URL=https://auth.oxy.so
