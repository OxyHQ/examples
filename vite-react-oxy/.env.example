# Oxy API base URL. Use production or your own Oxy API instance.
VITE_OXY_API_URL=https://api.oxy.so

# (Optional) Override the Oxy auth web app URL. Defaults to https://auth.oxy.so.
# VITE_OXY_AUTH_WEB_URL=https://auth.oxy.so
